<Window x:Class="Kursovai.Views.AdminGlavnoe"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Kursovai.Views"
        mc:Ignorable="d"
        Title="" Height="450" Width="800"  WindowStartupLocation="CenterScreen"  ResizeMode="NoResize"   >
    <Grid>
        <Grid.ColumnDefinitions>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*" />
            <RowDefinition Height="2*" />
            <RowDefinition Height="10*" />
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="3*" />
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="Торговый центр"/>
            <StackPanel  Grid.Row="0" Grid.Column="2">
                <Label Name="TimerY" Grid.Row="0" Grid.Column="2" FontSize="20" HorizontalContentAlignment="Center" VerticalContentAlignment="Center"  />
                <Label Name="TimerX" Grid.Row="0" Grid.Column="2" FontSize="12" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" />
            </StackPanel>
            <Button Grid.Column="3" Width="70" Height="20" Margin="10 0 10 30 " Content="вернуться" Click="Button_Click"/>
        </Grid>
        <DataGrid IsReadOnly="True" Margin="5" Grid.Row="2" Grid.RowSpan="2" Grid.ColumnSpan="4" Name="GridUchet" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="№" Width="30" Binding="{Binding Ключ}"/>
                <DataGridTextColumn Header="Задача" Width="*" Binding="{Binding НазваниеЗадачи}"/>
                <DataGridTextColumn Header="напровление" Width="*" Binding="{Binding КлючНаправление}"/>
                <DataGridTextColumn Header="проверка" Width="*" Binding="{Binding КлючСделанойРабочим}"/>
                <DataGridTextColumn Header="Описание" Width="*" Binding="{Binding Описание}"/>
                <DataGridTextColumn Header="дата" Width="*" Binding="{Binding DataT}"/>
                <DataGridTextColumn Header="проверка" Width="*" Binding="{Binding КлючПроверки}"/>
               
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
